import 'package:flutter/material.dart';

class ExistingProjectsButton extends StatelessWidget {
  const ExistingProjectsButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: const Center());
  }
}
